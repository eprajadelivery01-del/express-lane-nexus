import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eprajadelivery.admin',
  appName: 'PainelAdministrativo',
  webDir: 'dist'
};

export default config;
